// src/global.d.ts

// Declara la función global para que TypeScript la reconozca
declare function initializeStarRating(containerId: string, initialRating?: number, readonly?: boolean, eventName?: string): void;
